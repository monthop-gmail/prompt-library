# 🎙️ Meeting Notes Agent + RAG Pipeline
## คู่มือ Prompt สำหรับสร้างระบบบันทึกการประชุมด้วย Claude Artifacts

---

## ภาพรวมของระบบ

```
Audio/Text → [Agent บันทึกการประชุม] → Structured Notes → [Chunking + Embedding] → Vector DB → [RAG Query]
```

ระบบแบ่งเป็น 3 ส่วนหลัก:

| ส่วน | หน้าที่ | เทคโนโลยี |
|------|---------|-----------|
| **Agent บันทึก** | จดบันทึก สรุป แยก action items | Claude Artifact (React) + Anthropic API |
| **RAG Pipeline** | chunk → embed → store → retrieve | Python + LangChain / LlamaIndex |
| **Query Interface** | ค้นหาข้อมูลจากบันทึกเก่า | Claude Artifact + Anthropic API + Web Search |

---

## ส่วนที่ 1: Agent บันทึกการประชุม (Artifact)

### Prompt 1.1 — สร้าง Meeting Notes Agent UI

```
สร้าง React artifact เป็นแอป "Meeting Notes Agent" ที่มีฟีเจอร์:

1. ฟอร์มกรอกข้อมูลการประชุม:
   - ชื่อการประชุม, วันที่, ผู้เข้าร่วม (เพิ่มได้หลายคน)
   - ช่องวาง raw notes / transcript (textarea ใหญ่)

2. ปุ่ม "สรุปการประชุม" ที่เรียก Anthropic API ให้ Claude วิเคราะห์ raw notes แล้วสร้าง:
   - สรุปประเด็นหลัก (Executive Summary)
   - หัวข้อที่พูดคุย พร้อมรายละเอียด
   - Action Items: ใครทำอะไร กำหนดเมื่อไหร่
   - Decisions ที่ตัดสินใจแล้ว
   - Open Questions ที่ยังค้างอยู่

3. แสดงผลลัพธ์แบบ structured พร้อม:
   - tag/label สำหรับแต่ละ action item (priority: high/medium/low)
   - สถานะ: pending / in-progress / done
   - ปุ่ม copy JSON ที่จะ export ข้อมูลทั้งหมดเป็น JSON format
     พร้อมสำหรับส่งเข้า RAG pipeline

4. ใช้ persistent storage เก็บบันทึกข้ามเซสชัน

ออกแบบ UI สวยงาม ใช้โทนสี professional
dark theme ที่ดูทันสมัย
```

### Prompt 1.2 — เพิ่ม Audio Transcription (ขั้นสูง)

```
ปรับปรุง Meeting Notes Agent ให้รองรับ:

1. ปุ่ม "อัปโหลด Audio" รับไฟล์ .mp3, .wav, .m4a
   - ใช้ Web Speech API หรือ Whisper API ถอดเสียงเป็นข้อความ
   - แสดง transcript แบบ real-time

2. ปุ่ม "บันทึกเสียงสด" สำหรับบันทึกระหว่างประชุม
   - ใช้ MediaRecorder API
   - แสดง waveform visualization ขณะบันทึก

3. หลัง transcribe เสร็จ ให้ auto-fill ลงช่อง raw notes
   แล้วพร้อมให้กด "สรุปการประชุม" ต่อได้เลย
```

---

## ส่วนที่ 2: Structured Output Schema

### Prompt 2.1 — ออกแบบ JSON Schema สำหรับ RAG

```
ออกแบบ JSON Schema สำหรับข้อมูลบันทึกการประชุมที่จะส่งเข้า RAG pipeline
โดยต้องรองรับ:

1. Metadata: meeting_id, title, date, participants, duration, tags
2. Content chunks ที่เหมาะสำหรับ embedding:
   - executive_summary (chunk เดี่ยว)
   - topics[] แต่ละหัวข้อเป็น 1 chunk
   - action_items[] แต่ละ item เป็น 1 chunk
   - decisions[] แต่ละ decision เป็น 1 chunk
   - open_questions[]
3. แต่ละ chunk ต้องมี:
   - chunk_id
   - chunk_type (summary/topic/action/decision/question)
   - content (text สำหรับ embed)
   - metadata (meeting_id, date, participants ที่เกี่ยวข้อง)

ให้ตัวอย่าง JSON จริงพร้อมข้อมูลจำลอง
และอธิบายว่าทำไมถึงออกแบบ chunk แบบนี้
```

**ตัวอย่าง Output Schema:**

```json
{
  "meeting_id": "mtg_20260203_001",
  "title": "Sprint Planning Q1",
  "date": "2026-02-03T09:00:00+07:00",
  "participants": ["สมชาย", "สมหญิง", "วิทยา"],
  "duration_minutes": 60,
  "tags": ["sprint-planning", "q1-2026", "product"],
  "chunks": [
    {
      "chunk_id": "mtg_20260203_001_summary",
      "chunk_type": "summary",
      "content": "การประชุม Sprint Planning สำหรับ Q1 2026...",
      "metadata": {
        "meeting_id": "mtg_20260203_001",
        "date": "2026-02-03",
        "relevant_participants": ["สมชาย", "สมหญิง", "วิทยา"]
      }
    },
    {
      "chunk_id": "mtg_20260203_001_action_001",
      "chunk_type": "action_item",
      "content": "สมชาย: ออกแบบ database schema ภายในวันศุกร์ที่ 7 ก.พ. [priority: high]",
      "metadata": {
        "assignee": "สมชาย",
        "due_date": "2026-02-07",
        "priority": "high",
        "status": "pending",
        "meeting_id": "mtg_20260203_001"
      }
    }
  ]
}
```

---

## ส่วนที่ 3: RAG Pipeline (Python)

### Prompt 3.1 — สร้าง Ingestion Pipeline

```
เขียน Python script สำหรับ RAG ingestion pipeline ที่:

1. รับ JSON จาก Meeting Notes Agent
2. แยก chunks ตาม schema ที่ออกแบบไว้
3. สร้าง embedding ด้วย:
   - OpenAI text-embedding-3-small หรือ
   - Anthropic Voyager หรือ
   - sentence-transformers (local)
4. เก็บลง vector database:
   - ตัวเลือก A: ChromaDB (เริ่มต้นง่าย)
   - ตัวเลือก B: Pinecone (production)
   - ตัวเลือก C: Qdrant (self-hosted)
5. เก็บ metadata ครบถ้วนสำหรับ filtering

ใช้ LangChain เป็น framework
ให้โค้ดที่รันได้จริง พร้อม requirements.txt
```

### Prompt 3.2 — สร้าง Retrieval + Generation

```
เขียน Python module สำหรับ RAG retrieval ที่:

1. รับ query ภาษาไทยหรืออังกฤษ
2. ค้นหาจาก vector DB ด้วย:
   - semantic search (similarity)
   - metadata filter (วันที่, ผู้เข้าร่วม, ประเภท chunk)
   - hybrid search (semantic + keyword)
3. Re-rank ผลลัพธ์
4. ส่ง context ให้ Claude API สร้างคำตอบ
   พร้อม citation อ้างอิงว่ามาจากการประชุมไหน

ตัวอย่าง query ที่ต้องรองรับ:
- "สมชายมี action items อะไรค้างบ้าง?"
- "สรุปการตัดสินใจเรื่อง database ในเดือนที่แล้ว"
- "ใครรับผิดชอบเรื่อง API integration?"
```

---

## ส่วนที่ 4: Query Interface (Artifact)

### Prompt 4.1 — สร้าง Chat Interface สำหรับ Query

```
สร้าง React artifact เป็น "Meeting Knowledge Base" chat interface:

1. หน้า chat สำหรับถามคำถามเกี่ยวกับการประชุมเก่า
2. เรียก Anthropic API โดยใส่ system prompt:
   "คุณเป็น Meeting Assistant ที่มีข้อมูลบันทึกการประชุมทั้งหมด
    ตอบคำถามโดยอ้างอิงจากข้อมูลที่ให้มาเท่านั้น
    ระบุแหล่งที่มาทุกครั้ง (ชื่อประชุม + วันที่)"

3. Sidebar แสดง:
   - รายการประชุมล่าสุด
   - Filter ตามวันที่ / ผู้เข้าร่วม / tag
   - Action items ที่ยังค้าง (dashboard view)

4. แสดง citation แบบ clickable
   กดแล้วแสดงบันทึกต้นฉบับ

5. ใช้ persistent storage ดึงข้อมูลจากบันทึกที่เก็บไว้

สไตล์: clean, professional, dark/light mode toggle
```

---

## ส่วนที่ 5: System Prompt สำหรับ Agent

### Prompt 5.1 — System Prompt ของ Meeting Agent

```
ใช้ system prompt นี้ใน API call ของ artifact:

---
คุณคือ Meeting Notes Agent ผู้เชี่ยวชาญในการสรุปบันทึกการประชุม

หน้าที่ของคุณ:
1. วิเคราะห์ raw meeting notes/transcript
2. สร้างผลลัพธ์เป็น JSON ที่มีโครงสร้างชัดเจน

กฎการทำงาน:
- สรุปเป็นภาษาไทย ยกเว้นคำศัพท์เทคนิค
- Action item ต้องมี: ผู้รับผิดชอบ, สิ่งที่ต้องทำ, กำหนดส่ง
- ถ้าไม่มีกำหนดส่งชัดเจน ให้ระบุว่า "TBD"
- จัด priority โดยดูจาก urgency + impact
- แยก decisions (ตัดสินแล้ว) vs open questions (ยังไม่ตัดสิน)
- ใส่ tags อัตโนมัติตามเนื้อหา

ตอบเฉพาะ JSON เท่านั้น ไม่ต้องมีข้อความอื่น
ใช้ schema นี้:
{
  "executive_summary": "...",
  "topics": [{"title": "...", "detail": "...", "participants": [...]}],
  "action_items": [{"assignee": "...", "task": "...", "due": "...", "priority": "high|medium|low"}],
  "decisions": [{"decision": "...", "rationale": "...", "decided_by": "..."}],
  "open_questions": [{"question": "...", "raised_by": "..."}],
  "tags": [...]
}
---
```

---

## ส่วนที่ 6: Advanced — Full Pipeline Artifact

### Prompt 6.1 — All-in-One Demo

```
สร้าง React artifact ที่รวมทุกอย่างเป็นแอปเดียว:

Tab 1 - "บันทึก": ฟอร์มกรอก + สรุปการประชุม (เรียก Claude API)
Tab 2 - "คลังข้อมูล": แสดงประชุมทั้งหมดที่เก็บไว้ใน persistent storage
Tab 3 - "ค้นหา": chat interface ถามคำถาม (เรียก Claude API + web search)
Tab 4 - "Dashboard": สรุป action items, deadlines, statistics

ใช้ persistent storage เก็บข้อมูลทุกการประชุม
เมื่อค้นหาใน Tab 3 ให้ดึงข้อมูลจาก storage
ส่งเป็น context ให้ Claude API ตอบ

นี่คือ RAG แบบ simplified ที่ทำงานได้จริงใน artifact
โดยใช้ persistent storage แทน vector DB
และใช้ Claude API แทน embedding model
```

---

## เทคนิคเพิ่มเติม

### การ Chunk ที่ดีสำหรับ Meeting Notes

| วิธี | ข้อดี | เหมาะกับ |
|------|-------|----------|
| **By topic** | context ครบในแต่ละ chunk | ประชุมที่มีหลายวาระ |
| **By action item** | ค้นหา task ได้ตรง | ติดตาม action items |
| **By speaker** | รู้ว่าใครพูดอะไร | ประชุมที่ต้อง attribute |
| **Sliding window** | ไม่หลุด context | transcript ยาว ๆ |

### Embedding Tips สำหรับภาษาไทย

- ใช้ `text-embedding-3-small` ของ OpenAI (รองรับไทย)
- หรือ `multilingual-e5-large` จาก HuggingFace
- เพิ่ม "meeting context prefix" ก่อน embed เช่น:
  `"บันทึกการประชุม: {topic} วันที่ {date}: {content}"`
- ทำให้ retrieval แม่นขึ้นมาก

### Metadata Filtering ที่ควรมี

```python
# ตัวอย่างการ query พร้อม filter
results = vectorstore.similarity_search(
    query="action items ของสมชาย",
    filter={
        "chunk_type": "action_item",
        "assignee": "สมชาย",
        "status": "pending",
        "date": {"$gte": "2026-01-01"}
    },
    k=10
)
```

---

## ลำดับการสร้างที่แนะนำ

1. **เริ่มจาก Prompt 1.1** — สร้าง Agent UI ใน artifact ก่อน
2. **ทดสอบ Prompt 5.1** — ลอง system prompt กับ notes จริง
3. **ใช้ Prompt 2.1** — กำหนด JSON schema
4. **สร้าง Prompt 6.1** — All-in-One demo ที่ทำงานได้ใน artifact
5. **ขยายด้วย Prompt 3.1 + 3.2** — สร้าง Python pipeline สำหรับ production

> 💡 **Tips**: เริ่มจาก artifact demo ก่อนเสมอ
> ทดสอบ flow ให้เวิร์ค แล้วค่อยขยายไป production pipeline
