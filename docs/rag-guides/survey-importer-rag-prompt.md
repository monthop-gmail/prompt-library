# 📊 Survey CSV Importer → MCP RAG

## Prompt สำหรับสร้าง Artifact ตัว Import ข้อมูล Survey จาก CSV เข้า RAG System

---

## 🎯 Concept Overview

```
CSV File (Survey Results)
    ↓ upload / paste
[Artifact: Survey Importer]
    ↓ parse → validate → chunk → enrich metadata
[Claude API: Summarize + Embed-ready]
    ↓ structured JSON
[Persistent Storage: RAG Knowledge Base]
    ↓ query
[Claude API: Retrieval + Answer]
```

---

## Prompt 1 — ตัวหลัก: Survey CSV Importer + RAG

### Prompt (คัดลอกทั้งหมดไปใส่ Claude)

```
สร้าง React Artifact ชื่อ "Survey Data Importer → RAG"
เป็นเครื่องมือนำเข้าข้อมูลแบบสำรวจจากไฟล์ CSV แล้วจัดเก็บลง persistent storage
เพื่อใช้เป็น knowledge base สำหรับถาม-ตอบผ่าน Claude API (RAG pattern)

## โครงสร้างหลัก — 4 แท็บ

### Tab 1: "📥 นำเข้าข้อมูล" (Import)
- ปุ่ม upload ไฟล์ CSV + กล่อง paste ข้อมูล CSV โดยตรง
- ใช้ Papaparse สำหรับ parse CSV (รองรับ UTF-8 ภาษาไทย)
- แสดง preview ตารางข้อมูล 10 แถวแรกหลัง parse
- ให้ผู้ใช้ mapping คอลัมน์:
  - เลือกคอลัมน์ที่เป็น "คำถาม" (question_col)
  - เลือกคอลัมน์ที่เป็น "คำตอบ" (answer_col)
  - เลือกคอลัมน์ที่เป็น "ผู้ตอบ/กลุ่ม" (respondent_col) — optional
  - เลือกคอลัมน์ที่เป็น "วันที่" (date_col) — optional
  - เลือกคอลัมน์อื่นเป็น metadata เพิ่มเติม
- ปุ่ม "ตรวจสอบข้อมูล" — validate แล้วแสดงสรุป:
  - จำนวนแถวทั้งหมด, แถวที่มีปัญหา, คอลัมน์ที่ mapping แล้ว
- ปุ่ม "นำเข้า" — เริ่ม import pipeline

### Tab 2: "🔄 ประมวลผล" (Processing Pipeline)
เมื่อกด "นำเข้า" ให้แสดง pipeline ทำงานแบบ step-by-step:

**Step 1: Chunking Strategy**
- แต่ละแถวของ CSV → 1 chunk
- สร้าง chunk_id: `survey_{dataset_name}_{row_index}`
- สร้าง chunk_text: รวม question + answer + metadata เป็นข้อความเดียว
  เช่น: "คำถาม: {q} | คำตอบ: {a} | ผู้ตอบ: {r} | วันที่: {d}"

**Step 2: Metadata Enrichment**
- เรียก Claude API (claude-sonnet-4-20250514) 1 ครั้งต่อ batch (ส่งทีละ 20 chunks)
- Prompt ให้ Claude:
  - สร้าง tags/categories สำหรับแต่ละ chunk
  - สรุปประเด็นหลักของ batch นั้น (batch_summary)
  - ระบุ sentiment ถ้าเป็น open-ended response
- Response ต้องเป็น JSON เท่านั้น

**Step 3: Storage**
- เก็บลง persistent storage โครงสร้าง:
  ```
  Key: "rag:surveys"        → metadata ของทุก dataset ที่ import
  Key: "rag:ds_{id}"        → chunks ทั้งหมดของ dataset นั้น
  Key: "rag:ds_{id}:summary" → สรุปภาพรวมจาก Claude
  ```

- แสดง progress bar + สถานะแต่ละ step
- แสดง log ข้อความสั้นๆ ของแต่ละขั้นตอน

### Tab 3: "📚 คลังข้อมูล" (Knowledge Base)
- แสดงรายการ dataset ทั้งหมดที่ import แล้ว
- แต่ละ dataset แสดง: ชื่อ, จำนวน chunks, วันที่ import, tags
- กดเข้าไปดู:
  - ตารางข้อมูลทั้งหมด (searchable, sortable)
  - สรุปภาพรวม (batch_summary จาก Claude)
  - Tag cloud / category breakdown
- ปุ่ม "ลบ dataset" (ลบจาก persistent storage)
- ปุ่ม "Export JSON" (ดาวน์โหลด chunks + metadata เป็น JSON)

### Tab 4: "💬 ถาม-ตอบ" (RAG Query)
- Chat interface ภาษาไทย
- เลือก dataset ที่จะค้นหา (dropdown, เลือกได้หลาย dataset)
- เมื่อผู้ใช้ถามคำถาม:
  1. โหลด chunks จาก persistent storage ของ dataset ที่เลือก
  2. ใช้ keyword matching + Claude API reranking หา chunks ที่เกี่ยวข้อง
     (ส่ง query + chunk texts ให้ Claude เลือก top-5 ที่เกี่ยวข้องที่สุด)
  3. ส่ง context (top-5 chunks) + คำถามให้ Claude API ตอบ
  4. แสดงคำตอบ + อ้างอิง chunks ที่ใช้ (แสดง chunk_id, ข้อมูลต้นทาง)
- แสดงประวัติการถาม-ตอบในหน้าเดียวกัน (in-memory)

## ข้อมูลทดสอบ (Seed Data)
ฝัง sample CSV ไว้ในตัว artifact เป็นปุ่ม "ลองใช้ข้อมูลตัวอย่าง"
ข้อมูลตัวอย่าง: ผลสำรวจความพึงพอใจหลักสูตรสมาธิ

| ลำดับ | สาขา | หลักสูตร | คำถาม | คำตอบ | คะแนน | วันที่ |
|-------|-------|----------|-------|-------|-------|--------|
| 1 | สาขา 40 วัดตรีรัตนาราม | ชินนสาสมาธิ 1 | ท่านพึงพอใจภาพรวมหลักสูตรเพียงใด | พอใจมาก เข้าใจง่าย ปฏิบัติตามได้ทันที | 5 | 2568-01-15 |
| 2 | สาขา 269 วัดพระธาตุดอยสะเก็ด | ชินนสาสมาธิ 1 | ท่านพึงพอใจภาพรวมหลักสูตรเพียงใด | ดีมาก สถานที่สงบ อาจารย์อธิบายชัดเจน | 5 | 2568-01-15 |
| 3 | สาขา 173 วัดหนามแดง | ครูสมาธิ รุ่น 53 | การเรียนเหมาะสมกับเวลาหรือไม่ | เหมาะสม แต่อยากให้มีคาบเสริมวันธรรมดา | 4 | 2568-02-01 |
| 4 | สาขา 5 วัดดอนรัก | ครูสมาธิ รุ่น 53 | สิ่งที่ได้เรียนรู้มากที่สุด | การทำสมาธิแบบเคลื่อนไหว เดินจงกรม | - | 2568-02-01 |
| 5 | สาขา 312 วัดป่ากาญจนาภิเษก | ญาณสาสมาธิ ออนไลน์ | ปัญหาที่พบในการเรียนออนไลน์ | สัญญาณอินเทอร์เน็ตไม่เสถียร | 3 | 2568-01-20 |
| 6 | สาขา 1 บ้านอาจารย์อำนวย | ชินนสาสมาธิ 1 | ท่านพึงพอใจภาพรวมหลักสูตรเพียงใด | ดี แต่ห้องแคบไปนิด | 3 | 2568-01-22 |
| 7 | สาขา 117 วัดโสภาราม | วิทันตสาสมาธิ ออนไลน์ | ข้อเสนอแนะเพิ่มเติม | อยากให้มีกลุ่ม LINE สำหรับแลกเปลี่ยนประสบการณ์ | - | 2568-01-25 |
| 8 | สาขา 309 วัดป่าร้อยปีฯ | ครูสมาธิ รุ่น 53 | การเรียนเหมาะสมกับเวลาหรือไม่ | เหมาะสมดี เรียนเฉพาะเสาร์-อาทิตย์ลงตัว | 5 | 2568-02-01 |

เก็บเป็น constant ใน artifact โดยแปลงเป็น CSV string

## Design & Theme
- โทนสี: น้ำเงินเข้ม (#1a237e) + ทอง (#d4a843) + ครีม (#fff8e7)
- ฟอนต์: ใช้ Google Fonts — "Sarabun" สำหรับภาษาไทย
  โหลดจาก: https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;600;700&display=swap
- สไตล์: สะอาด เรียบ professional แบบ dashboard
- มี icon ใช้ lucide-react
- Responsive ใช้งานได้ทั้ง desktop และ mobile

## เทคนิคสำคัญ

### การเรียก Claude API
```javascript
const response = await fetch("https://api.anthropic.com/v1/messages", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    model: "claude-sonnet-4-20250514",
    max_tokens: 1000,
    messages: [{ role: "user", content: promptText }]
  })
});
const data = await response.json();
const text = data.content.map(i => i.text || "").join("\n");
```

### การใช้ Persistent Storage
```javascript
// บันทึก
await window.storage.set("rag:surveys", JSON.stringify(datasetList));
await window.storage.set(`rag:ds_${id}`, JSON.stringify(chunks));

// อ่าน
const result = await window.storage.get("rag:surveys");
const surveys = result ? JSON.parse(result.value) : [];
```

### การ parse CSV ด้วย Papaparse
```javascript
import * as Papa from 'papaparse';
const parsed = Papa.parse(csvString, {
  header: true,
  skipEmptyLines: true,
  encoding: "UTF-8"
});
```

### RAG Retrieval (Simplified)
ไม่ใช้ vector DB จริง — ใช้ Claude API เป็น reranker แทน:
```javascript
const rerankPrompt = `จากข้อมูลต่อไปนี้ ให้เลือก 5 รายการที่เกี่ยวข้องกับคำถามมากที่สุด
คำถาม: "${query}"
ข้อมูล:
${chunks.map((c,i) => `[${i}] ${c.text}`).join('\n')}
ตอบเป็น JSON array ของ index เท่านั้น เช่น [2,5,0,7,3]`;
```

## ห้ามทำ
- ห้ามใช้ localStorage หรือ sessionStorage
- ห้ามใช้ <form> tag
- ห้ามสร้างไฟล์แยก (ทุกอย่างอยู่ใน .jsx ไฟล์เดียว)
- ห้ามใช้ภาษาอังกฤษใน UI (ยกเว้น technical term)
```

---

## Prompt 2 — เวอร์ชันเน้น Survey Analysis

### (ใช้เมื่อต้องการเน้นการวิเคราะห์ผลสำรวจมากกว่า RAG ทั่วไป)

```
สร้าง React Artifact ชื่อ "วิเคราะห์ผลสำรวจ + RAG"
เป็นเครื่องมือวิเคราะห์ข้อมูลแบบสำรวจจาก CSV พร้อมระบบถาม-ตอบอัจฉริยะ

## 5 แท็บ

### Tab 1: "📥 อัพโหลด"
- Upload CSV / Paste CSV / ใช้ข้อมูลตัวอย่าง
- Parse ด้วย Papaparse, preview 10 แถว
- Column mapping: คำถาม, คำตอบ, คะแนน, สาขา, หลักสูตร, วันที่
- Validate → Import → เก็บ persistent storage

### Tab 2: "📊 ภาพรวม" (Dashboard)
หลัง import แล้ว ให้เรียก Claude API วิเคราะห์ข้อมูลทั้ง dataset แล้วแสดง:

- KPI Cards:
  - จำนวนผู้ตอบทั้งหมด
  - คะแนนเฉลี่ย (ถ้ามี)
  - จำนวนสาขาที่เกี่ยวข้อง
  - จำนวนหลักสูตร
- กราฟ (ใช้ recharts):
  - Bar chart: คะแนนเฉลี่ยแยกตามสาขา
  - Pie chart: สัดส่วนหลักสูตร
  - Bar chart: จำนวนผู้ตอบแยกตามคำถาม
- Claude AI Summary:
  - สรุปภาพรวม 3-5 ประโยค
  - จุดเด่นที่พบ
  - ข้อควรปรับปรุง
  - ข้อเสนอแนะ

### Tab 3: "💬 ถาม-ตอบ RAG"
- Chat interface
- เมื่อถามคำถาม:
  1. โหลด chunks จาก persistent storage
  2. ส่ง query + chunks ให้ Claude API ค้นหา relevant chunks (reranking)
  3. ส่ง context + query ให้ Claude API ตอบ
  4. แสดงคำตอบ + citations (อ้างอิงข้อมูลต้นทาง)

ตัวอย่างคำถามที่ควร handle:
- "สาขาไหนได้คะแนนสูงสุด?"
- "ปัญหาที่พบบ่อยในหลักสูตรออนไลน์คืออะไร?"
- "มีข้อเสนอแนะอะไรเกี่ยวกับเวลาเรียนบ้าง?"
- "สรุปความคิดเห็นของผู้เรียนครูสมาธิรุ่น 53"

### Tab 4: "🏷️ Tags & Categories"
- แสดง tag cloud จากข้อมูลที่ Claude สร้างไว้ตอน import
- กดที่ tag → กรองดูเฉพาะ chunks ที่มี tag นั้น
- Sentiment breakdown (positive / neutral / negative)
- Category tree: หลักสูตร > สาขา > คำถาม

### Tab 5: "⚙️ จัดการ"
- รายการ dataset ทั้งหมด
- ลบ dataset
- Export: JSON, CSV (แบบมี metadata เพิ่ม)
- ล้างข้อมูลทั้งหมด

## Seed Data
(ใช้ตาราง 8 แถวเดียวกับ Prompt 1)

## Theme & Design
- โทนสี: indigo (#3f51b5) + amber (#ffc107) + white (#fafafa)
- ฟอนต์: "Sarabun" จาก Google Fonts
- สไตล์: Material-inspired, card-based, clean dashboard
- Charts: ใช้ recharts library
- Icons: lucide-react

## Technical Notes
- ใช้ Papaparse สำหรับ CSV
- ใช้ recharts สำหรับ charts
- ใช้ Claude API (claude-sonnet-4-20250514) สำหรับ:
  - วิเคราะห์ข้อมูลตอน import
  - RAG retrieval (reranking + answering)
  - สร้าง dashboard summary
- ใช้ window.storage (persistent storage) สำหรับเก็บข้อมูล
- ห้ามใช้ localStorage/sessionStorage
- ห้ามใช้ <form> tag
- ทุกอย่างอยู่ในไฟล์ .jsx เดียว
```

---

## Prompt 3 — เวอร์ชัน Generic (ไม่เจาะจง Survey สมาธิ)

### (สำหรับใช้กับ CSV ใดก็ได้)

```
สร้าง React Artifact: "CSV → RAG Knowledge Base"

เครื่องมือ universal สำหรับ import ข้อมูลจาก CSV ใดก็ได้
แปลงเป็น knowledge base ใน persistent storage
แล้วให้ถาม-ตอบข้อมูลผ่าน Claude API ได้

## 3 แท็บ

### Tab 1: "Import"
- Upload CSV หรือ paste CSV text
- ใช้ Papaparse parse (header: true, skipEmptyLines: true)
- แสดง preview table (10 แถวแรก)
- ให้เลือก chunking strategy:
  - [x] 1 แถว = 1 chunk (default)
  - [ ] Group by column (เลือก column ที่จะ group)
  - [ ] Combine all = 1 chunk
- ให้เลือก content columns: คอลัมน์ไหนจะเป็นเนื้อหาหลักของ chunk
- ให้เลือก metadata columns: คอลัมน์ไหนจะเป็น metadata (ใช้ filter ตอน query)
- ให้ตั้งชื่อ dataset
- กดปุ่ม "Process & Store"
- Pipeline:
  1. สร้าง chunks ตาม strategy ที่เลือก
  2. (Optional) เรียก Claude API สรุป dataset + สร้าง tags
  3. เก็บลง persistent storage

### Tab 2: "Knowledge Base"
- รายการ dataset ทั้งหมด (cards)
- แต่ละ dataset: ชื่อ, จำนวน chunks, วันที่ import
- กดดู: table view ของ chunks ทั้งหมด + search + filter by metadata
- ลบ dataset
- Export JSON

### Tab 3: "Ask"
- Chat interface (ภาษาไทย/อังกฤษ)
- เลือก dataset (multi-select)
- ถามคำถาม → RAG pipeline:
  1. Load chunks จาก storage
  2. ถ้า chunks <= 50: ส่งทั้งหมดให้ Claude เป็น context
  3. ถ้า chunks > 50: ใช้ keyword filter ก่อน → ส่ง top-50 ให้ Claude
  4. Claude ตอบ + อ้างอิง
- แสดง: คำตอบ + source chunks ที่อ้างอิง

## Design
- Dark theme: #0f172a background, #e2e8f0 text, #3b82f6 accent
- ฟอนต์: "Sarabun" (Thai) จาก Google Fonts
- Minimal, modern, developer-tool aesthetic
- Icons: lucide-react

## Technical
- Papaparse for CSV
- Claude API (claude-sonnet-4-20250514)
- window.storage for persistence
- ไม่ใช้ localStorage, ไม่ใช้ <form>
- ไฟล์ .jsx เดียว
```

---

## 📐 Data Schema Reference

### Chunk Structure

```json
{
  "chunk_id": "survey_satisfaction_001",
  "chunk_type": "survey_response",
  "text": "คำถาม: ท่านพึงพอใจภาพรวมหลักสูตรเพียงใด | คำตอบ: พอใจมาก เข้าใจง่าย ปฏิบัติตามได้ทันที | คะแนน: 5 | สาขา: สาขา 40 วัดตรีรัตนาราม | หลักสูตร: ชินนสาสมาธิ 1",
  "metadata": {
    "dataset_id": "satisfaction_2568",
    "dataset_name": "ผลสำรวจความพึงพอใจ 2568",
    "row_index": 0,
    "branch": "สาขา 40 วัดตรีรัตนาราม",
    "course": "ชินนสาสมาธิ 1",
    "score": 5,
    "date": "2568-01-15",
    "tags": ["ความพึงพอใจ", "เข้าใจง่าย", "ปฏิบัติได้"],
    "sentiment": "positive"
  },
  "imported_at": "2026-02-03T10:30:00Z"
}
```

### Dataset Registry (rag:surveys)

```json
[
  {
    "id": "satisfaction_2568",
    "name": "ผลสำรวจความพึงพอใจ 2568",
    "chunk_count": 156,
    "columns": ["ลำดับ", "สาขา", "หลักสูตร", "คำถาม", "คำตอบ", "คะแนน", "วันที่"],
    "content_cols": ["คำถาม", "คำตอบ"],
    "metadata_cols": ["สาขา", "หลักสูตร", "คะแนน", "วันที่"],
    "imported_at": "2026-02-03T10:30:00Z",
    "summary": "ผลสำรวจความพึงพอใจจาก 8 สาขา 3 หลักสูตร คะแนนเฉลี่ย 4.2/5..."
  }
]
```

### Storage Key Patterns

| Key | เนื้อหา | Shared? |
|-----|---------|---------|
| `rag:surveys` | รายการ dataset ทั้งหมด (registry) | false |
| `rag:ds_{id}` | chunks ทั้งหมดของ dataset | false |
| `rag:ds_{id}:summary` | สรุปจาก Claude API | false |
| `rag:ds_{id}:tags` | tag index สำหรับ filter | false |

---

## 🧠 Claude API Prompt Templates

### Template 1: Dataset Analysis (ใช้ตอน import)

```
วิเคราะห์ข้อมูลแบบสำรวจต่อไปนี้:

{chunk_texts ส่งเป็น batch 20 รายการ}

ตอบเป็น JSON เท่านั้น (ไม่มี markdown, ไม่มี backtick) ตามโครงสร้างนี้:
{
  "batch_summary": "สรุปภาพรวมของข้อมูลชุดนี้ 2-3 ประโยค",
  "items": [
    {
      "index": 0,
      "tags": ["tag1", "tag2"],
      "sentiment": "positive|neutral|negative",
      "category": "หมวดหมู่ที่เหมาะสม"
    }
  ]
}
```

### Template 2: RAG Reranking (ใช้ตอน query)

```
จากข้อมูลต่อไปนี้ ให้เลือก 5 รายการที่เกี่ยวข้องกับคำถามมากที่สุด
เรียงจากเกี่ยวข้องมากสุดไปน้อยสุด

คำถาม: "{user_query}"

ข้อมูล:
{chunks.map((c,i) => `[${i}] ${c.text}`).join('\n')}

ตอบเป็น JSON array ของ index เท่านั้น เช่น [2,5,0,7,3]
ไม่ต้องอธิบายเพิ่มเติม
```

### Template 3: RAG Answer Generation (ใช้ตอนตอบ)

```
คุณเป็นผู้ช่วยวิเคราะห์ข้อมูลแบบสำรวจ
ใช้ข้อมูลอ้างอิงต่อไปนี้เท่านั้นในการตอบ ห้ามแต่งเพิ่ม

ข้อมูลอ้างอิง:
{relevant_chunks.map((c,i) => `[${i+1}] ${c.text}`).join('\n\n')}

คำถาม: "{user_query}"

ตอบเป็นภาษาไทย กระชับ ชัดเจน
ถ้าข้อมูลไม่เพียงพอให้บอกตรงๆ
อ้างอิงข้อมูลโดยใช้ [หมายเลข] ท้ายประโยค
```

---

## ⚡ Quick-Start: Copy-Paste Prompt

สำหรับผู้ที่อยากได้ prompt สั้นๆ ก็อปวางแล้วใช้ได้เลย:

```
สร้าง React Artifact (.jsx) เป็นตัว import CSV เข้า RAG system

ฟีเจอร์:
1. Upload/paste CSV → parse ด้วย Papaparse
2. ให้ mapping คอลัมน์ (content vs metadata)
3. แปลงแต่ละแถวเป็น chunk → เก็บ persistent storage (window.storage)
4. เรียก Claude API วิเคราะห์ + สร้าง tags
5. Tab ถาม-ตอบ: โหลด chunks → Claude rerank → Claude ตอบ + อ้างอิง

ใช้: Papaparse, lucide-react, recharts
Theme: น้ำเงินเข้ม + ทอง + ครีม, ฟอนต์ Sarabun, UI ภาษาไทย
ห้าม: localStorage, <form>, ไฟล์แยก

ฝัง sample data ผลสำรวจความพึงพอใจหลักสูตรสมาธิ 8 แถว
```

---

## 📋 Checklist ก่อนใช้งาน

- [ ] คัดลอก prompt ที่ต้องการ (Prompt 1, 2, 3, หรือ Quick-Start)
- [ ] วางใน Claude chat (claude.ai หรือ Claude app)
- [ ] รอ Claude สร้าง Artifact (.jsx)
- [ ] ทดสอบด้วย "ลองใช้ข้อมูลตัวอย่าง"
- [ ] ทดสอบ upload CSV จริง
- [ ] ทดสอบถาม-ตอบใน Tab RAG Query
- [ ] ตรวจสอบว่า persistent storage เก็บข้อมูลได้
- [ ] ปิดแล้วเปิดใหม่ — ข้อมูลยังอยู่

---

## 🔧 Troubleshooting

| ปัญหา | สาเหตุ | แก้ไข |
|--------|--------|-------|
| ภาษาไทยเป็น ??? | CSV encoding ไม่ใช่ UTF-8 | แปลง CSV เป็น UTF-8 ก่อน upload |
| Claude API ไม่ตอบ | prompt ส่ง JSON แต่ได้ text กลับมา | เพิ่ม "ตอบเป็น JSON เท่านั้น" ใน prompt |
| Storage เต็ม | ข้อมูลเกิน 5MB ต่อ key | แบ่ง chunks เป็นหลาย keys |
| Chunks มากเกินไป | CSV มี 1000+ แถว | ใช้ keyword filter ก่อนส่ง Claude |
| Artifact ค้าง | เรียก API พร้อมกันมากเกินไป | batch ทีละ 20, ใส่ delay 500ms |
