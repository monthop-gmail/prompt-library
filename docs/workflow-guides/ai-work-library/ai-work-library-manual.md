# AI Work Library System — คู่มือฉบับสมบูรณ์

## แนวคิดหลัก

ระบบนี้เปลี่ยน **เครดิต Claude AI** ให้กลายเป็น **ทรัพย์สินสะสม** (Prompt Assets) โดยใช้ GitHub เป็นศูนย์กลาง เก็บรวบรวม Prompt ในรูปแบบ Markdown ที่ AI แปลงเป็นโค้ดได้ทันที

**หลักการสำคัญ:** Prompt คือทรัพย์สิน — ยิ่ง AI เก่งขึ้นทุกเดือน Prompt เดิมก็ให้ผลลัพธ์ดีขึ้น แค่เปลี่ยน model ก็อัพเกรดงานทั้ง library ได้เลย

---

## สถาปัตยกรรมระบบ

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                     │
│                   (work-library/main)                    │
│                                                         │
│  ┌──────────┐   ┌──────────────┐   ┌────────────────┐  │
│  │  ideas/   │   │ existing-code/│   │   templates/   │  │
│  │  (Backlog)│   │ (Legacy code)│   │(Prompt formats)│  │
│  └────┬─────┘   └──────┬───────┘   └───────┬────────┘  │
│       │                │                    │           │
│       └────────┬───────┘                    │           │
│                ▼                            │           │
│  ┌─────────────────────────────┐            │           │
│  │     PHASE 1: Collect        │◄───────────┘           │
│  │  รวบรวม → Markdown Prompts  │                        │
│  └──────────┬──────────────────┘                        │
│             ▼                                           │
│  ┌─────────────────────────────┐                        │
│  │   PHASE 2: AI Transform     │                        │
│  │  Claude แปลงเป็น:           │                        │
│  │  ├── poc.md  (Proof of Concept)                      │
│  │  ├── mvp.md  (Minimum Viable Product)                │
│  │  ├── test.md (Test Suite)                            │
│  │  └── docs.md (Documentation)                         │
│  └──────────┬──────────────────┘                        │
│             ▼                                           │
│  ┌─────────────────────────────┐                        │
│  │   PHASE 3: Fork & Evolve    │                        │
│  │  fork → ปรับแต่ง → PR กลับ   │                        │
│  └─────────────────────────────┘                        │
│                                                         │
│  ┌──── fork/client-A ────┐   ┌── fork/experiment ──┐   │
│  │  ลูกค้า A (locked ver) │   │  ทดลอง prompt ใหม่  │   │
│  └───────────────────────┘   └─────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Phase 1: รวบรวม & จัดเก็บ (Collect)

### โครงสร้าง Repository

```
work-library/
├── README.md                    ← ภาพรวมทั้งหมด
├── ideas/                       ← ไอเดียที่ยังไม่ได้ทำ
│   ├── chatbot-line.md
│   ├── inventory-system.md
│   └── dashboard-analytics.md
├── existing-code/               ← โค้ดเก่าแปลงเป็น prompt
│   ├── pos-system.md
│   ├── crm-api.md
│   └── report-generator.md
├── prompts/                     ← prompt พร้อมใช้ (ผลลัพธ์ Phase 2)
│   ├── chatbot-line/
│   │   ├── poc.md
│   │   ├── mvp.md
│   │   ├── test.md
│   │   └── docs.md
│   └── pos-system/
│       ├── poc.md
│       ├── mvp.md
│       ├── test.md
│       └── docs.md
├── templates/                   ← เทมเพลตสำหรับสร้าง prompt ใหม่
│   ├── idea-template.md
│   ├── code-to-prompt-template.md
│   └── project-brief-template.md
└── .github/
    └── PULL_REQUEST_TEMPLATE.md ← template สำหรับ PR
```

### Template: บันทึกไอเดียใหม่ (idea-template.md)

```markdown
# 📋 Project: [ชื่อโปรเจค]

## Meta
- **category:** [chatbot / web-app / api / automation / dashboard]
- **status:** idea
- **priority:** [high / medium / low]
- **estimated-value:** [คุ้มค่าเพียงไหน 1-5]
- **created:** YYYY-MM-DD

## Problem
อธิบายปัญหาที่ต้องการแก้ — ลูกค้าเจอปัญหาอะไร?

## Solution
แนวทางแก้ปัญหาคร่าวๆ

## Requirements
- [ ] Requirement 1
- [ ] Requirement 2
- [ ] Requirement 3

## Tech Stack (ถ้าทราบ)
- Frontend:
- Backend:
- Database:
- APIs:

## Notes
บันทึกเพิ่มเติม, ลิงก์อ้างอิง, ตัวอย่างที่คล้ายกัน
```

### Template: แปลงโค้ดเก่าเป็น Prompt (code-to-prompt-template.md)

```markdown
# 🔄 Legacy Code: [ชื่อระบบเดิม]

## Meta
- **original-language:** [Python / PHP / Java / ...]
- **status:** existing-code
- **last-updated:** YYYY-MM-DD

## What It Does
อธิบายว่าโค้ดเดิมทำอะไร

## Key Features
1. Feature A — รายละเอียด
2. Feature B — รายละเอียด

## Architecture
อธิบายโครงสร้างคร่าวๆ (endpoints, database schema, flow)

## Pain Points
- ปัญหาของโค้ดเดิมที่อยากแก้
- สิ่งที่อยากเพิ่ม

## Code Snippets (ส่วนสำคัญ)
```[language]
// วางโค้ดส่วนที่ AI ต้องเข้าใจ logic
```

## Desired Improvements
- อยากให้ AI ปรับปรุงอะไรบ้าง
```

---

## Phase 2: AI แปลงเป็น Project Prompts

### วิธีใช้ Claude AI สร้าง Project Prompts

นำ Markdown จาก Phase 1 ป้อนให้ Claude แล้วสั่งให้สร้าง prompt ตาม template ด้านล่าง

### Prompt สั่ง Claude สร้าง POC

```
จากข้อมูลโปรเจคนี้ [วาง idea.md] ช่วยสร้าง POC prompt ที่:
1. เน้นทดสอบ core concept ให้เร็วที่สุด
2. ใช้ tech stack ง่ายที่สุด
3. ไม่ต้อง production-ready
4. มี step-by-step ชัดเจนที่ AI สามารถ generate code ได้ทั้งหมด

บันทึกในรูปแบบ Markdown ตาม template นี้:
[วาง poc-template.md]
```

### Template: POC Prompt (poc.md)

```markdown
# 🧪 POC: [ชื่อโปรเจค]

## Meta
- **type:** poc
- **parent:** ../ideas/[ชื่อ].md
- **ai-model:** claude-sonnet-4-5
- **generated:** YYYY-MM-DD
- **generation-count:** 1

## Objective
ทดสอบว่า [แนวคิดหลัก] เป็นไปได้จริงหรือไม่

## Scope (ทำแค่นี้)
- ✅ [สิ่งที่ทำ]
- ✅ [สิ่งที่ทำ]
- ❌ [สิ่งที่ยังไม่ทำ — รอ MVP]

## Prompt for AI
> คัดลอกส่วนนี้ทั้งหมดไปวางใน Claude

สร้าง [ประเภทงาน] ที่ [ทำอะไร] โดยมีรายละเอียดดังนี้:

**โครงสร้าง:**
[อธิบาย structure]

**Logic หลัก:**
[อธิบาย core logic]

**ข้อจำกัด:**
- ใช้ [tech stack]
- ไม่ต้อง auth/security
- ข้อมูลตัวอย่าง hardcode ได้

## Success Criteria
- [ ] ทำ [x] ได้สำเร็จ
- [ ] แสดงผลลัพธ์ [y] ถูกต้อง

## Results Log
| Version | Date       | Model              | Result | Notes     |
|---------|------------|--------------------|--------|-----------|
| v1      | YYYY-MM-DD | claude-sonnet-4-5  | ✅/❌  | [บันทึก]  |
```

### Template: MVP Prompt (mvp.md)

```markdown
# 🚀 MVP: [ชื่อโปรเจค]

## Meta
- **type:** mvp
- **parent:** ./poc.md
- **ai-model:** claude-sonnet-4-5
- **generated:** YYYY-MM-DD

## What's New from POC
- เพิ่ม [feature A]
- ปรับปรุง [component B]
- เพิ่ม error handling

## Prompt for AI
> คัดลอกส่วนนี้ทั้งหมดไปวางใน Claude พร้อมแนบ POC code

จาก POC ที่แนบมา ต่อยอดเป็น MVP โดย:

**เพิ่มเติม:**
1. [Feature ใหม่ + รายละเอียด]
2. [Feature ใหม่ + รายละเอียด]

**ปรับปรุง:**
1. [สิ่งที่ต้องแก้จาก POC]
2. [สิ่งที่ต้อง optimize]

**Production Requirements:**
- Error handling ครบ
- Input validation
- Logging
- Environment variables

## Deployment Notes
[วิธี deploy]
```

### Template: Test Prompt (test.md)

```markdown
# 🧪 Tests: [ชื่อโปรเจค]

## Meta
- **type:** test
- **parent:** ./mvp.md
- **ai-model:** claude-sonnet-4-5

## Prompt for AI
> แนบ MVP code แล้ววาง prompt นี้

สร้างชุด test สำหรับโค้ดที่แนบมา:

1. **Unit Tests** — ทดสอบแต่ละ function
2. **Integration Tests** — ทดสอบ flow หลัก
3. **Edge Cases** — กรณีพิเศษ, input ผิดปกติ

**Framework:** [jest / pytest / ...]
**Coverage target:** 80%+

ครอบคลุม:
- Happy path ทุก feature
- Error cases ทุกจุดที่รับ input
- Boundary values
```

### Template: Docs Prompt (docs.md)

```markdown
# 📚 Documentation: [ชื่อโปรเจค]

## Meta
- **type:** docs
- **parent:** ./mvp.md
- **ai-model:** claude-sonnet-4-5

## Prompt for AI
> แนบ MVP code แล้ววาง prompt นี้

สร้างเอกสารสำหรับโค้ดที่แนบมา ประกอบด้วย:

1. **README.md** — ภาพรวม, วิธีติดตั้ง, วิธีใช้งาน
2. **API.md** — endpoints ทั้งหมด (ถ้ามี)
3. **ARCHITECTURE.md** — โครงสร้างระบบ, data flow
4. **CHANGELOG.md** — บันทึกการเปลี่ยนแปลง

**สไตล์:**
- ภาษาไทย + technical terms ภาษาอังกฤษ
- มีตัวอย่าง code ทุกส่วน
- มี diagram (mermaid) อธิบาย flow
```

---

## Phase 3: Fork & ต่อยอด

### Git Workflow

```
main (library หลัก)
 │
 ├── fork → client-A/main
 │   ├── ลูกค้า A ใช้ version นี้
 │   └── ไม่ต้อง update ถ้าไม่จำเป็น
 │
 ├── fork → client-B/main
 │   └── ลูกค้า B version แยก
 │
 ├── fork → experiment/new-model
 │   ├── ทดลอง prompt กับ AI model ใหม่
 │   └── ถ้าดี → PR กลับ main
 │
 └── fork → experiment/v2-rewrite
     ├── เขียน prompt ใหม่ทั้งหมด
     └── ถ้าดีกว่า → PR กลับ main
```

### กฎการ PR กลับ main

เมื่อ fork ได้ผลลัพธ์ดี ให้สร้าง Pull Request กลับ main โดยต้องมี:

1. **อธิบายว่าเปลี่ยนอะไร** — prompt ใหม่ดีกว่าเดิมอย่างไร
2. **แนบผลลัพธ์** — output จาก AI ก่อน/หลัง
3. **ระบุ AI model** — ใช้ model อะไร version อะไร
4. **ไม่กระทบ prompt อื่น** — เปลี่ยนเฉพาะที่เกี่ยวข้อง

### PR Template (.github/PULL_REQUEST_TEMPLATE.md)

```markdown
## สรุปการเปลี่ยนแปลง
[อธิบายสั้นๆ]

## ประเภท
- [ ] Prompt ใหม่
- [ ] อัพเกรด Prompt เดิม
- [ ] แก้ไข Template
- [ ] เพิ่ม idea

## AI Model ที่ทดสอบ
- Model:
- Version:

## ผลลัพธ์
- ก่อน: [ผลลัพธ์เดิม]
- หลัง: [ผลลัพธ์ใหม่]

## Checklist
- [ ] ทดสอบ generate code แล้ว
- [ ] Success criteria ผ่าน
- [ ] อัพเดท Results Log
```

---

## กลยุทธ์ใช้เครดิต Claude ให้คุ้ม

### DO — สิ่งที่ควรทำ

**ใช้ Prompt ซ้ำ ไม่พิมพ์ใหม่** — เก็บ prompt ที่ได้ผลดีใน library แล้วใช้ซ้ำ ปรับแค่ส่วนที่ต่างกัน ประหยัดเครดิตได้มาก

**Batch งาน** — รวมงานหลายอย่างเป็นชุดเดียว เช่น สั่ง Claude สร้าง POC + Test + Docs พร้อมกัน ดีกว่าสั่งทีละอย่าง

**เริ่มจาก POC ก่อนเสมอ** — อย่ากระโดดไป MVP ทดสอบแนวคิดด้วย POC ก่อน ถ้า concept ไม่เวิร์ค จะได้ไม่เสียเครดิตเปล่ากับ MVP

**บันทึกทุกผลลัพธ์** — แม้ output ไม่สมบูรณ์ก็เก็บไว้ใน Results Log เพราะวิเคราะห์ย้อนหลังได้ว่า prompt ไหนต้องปรับตรงไหน

### DON'T — สิ่งที่ไม่ควรทำ

**อย่าพิมพ์ prompt ซ้ำจากความจำ** — คัดลอกจาก library เสมอ ป้องกันผิดพลาดและได้ผลลัพธ์ consistent

**อย่าข้ามการบันทึก** — ทุกครั้งที่ได้ output ต้องบันทึกลง Results Log ถ้าไม่บันทึก prompt จะไม่ปรับปรุง

**อย่าอัพเกรดให้ลูกค้าโดยไม่จำเป็น** — เมื่ออัพเกรด prompt ใน main แล้ว fork ของลูกค้าอยู่ version เดิมได้ อัพเกรดเมื่อลูกค้าต้องการเท่านั้น

---

## ขั้นตอนเริ่มต้นใช้งาน (Quick Start)

### Step 1: สร้าง Repository

```bash
mkdir work-library && cd work-library
git init

mkdir -p ideas existing-code prompts templates .github
```

### Step 2: คัดลอก Templates

คัดลอก template files ทั้งหมดจากคู่มือนี้ไปใส่ในโฟลเดอร์ `templates/`

### Step 3: เริ่มเก็บ Ideas

ใช้ `idea-template.md` สร้างไฟล์สำหรับทุกไอเดียที่มี วางใน `ideas/`

### Step 4: แปลงโค้ดเก่า

ใช้ `code-to-prompt-template.md` แปลงโค้ดเดิมที่อยากนำกลับมาใช้ วางใน `existing-code/`

### Step 5: ให้ Claude สร้าง Project Prompts

เปิด Claude → วาง idea หรือ existing-code markdown → สั่งสร้าง POC prompt → เก็บผลลัพธ์ใน `prompts/[project-name]/poc.md`

### Step 6: วน Loop

ทำ POC → ได้ผล → ทำ MVP → ทดสอบ → เขียน Docs → เก็บทุกอย่างใน library → ทำโปรเจคต่อไป

---

## คำสั่ง Git ที่ใช้บ่อย

```bash
# สร้าง fork สำหรับลูกค้า
git checkout -b client/company-name
git push origin client/company-name

# สร้าง fork สำหรับทดลอง
git checkout -b experiment/description
git push origin experiment/description

# PR กลับ main (ผ่าน GitHub UI หรือ CLI)
gh pr create --base main --head experiment/description \
  --title "Upgrade: chatbot prompt v2" \
  --body "ผลลัพธ์ดีขึ้น 40% เมื่อใช้กับ claude-sonnet-4-5"

# ดู branches ทั้งหมด
git branch -a
```

---

## Versioning Strategy

ใช้ `Results Log` ในแต่ละ prompt file เป็นตัวติดตาม version:

| ระดับ | ใช้เมื่อ | ตัวอย่าง |
|-------|---------|---------|
| **Major (v2.0)** | เปลี่ยน approach ทั้งหมด | เขียน prompt ใหม่ตั้งแต่ต้น |
| **Minor (v1.1)** | เพิ่ม feature หรือปรับปรุง | เพิ่ม error handling |
| **Patch (v1.0.1)** | แก้ bug เล็กน้อย | แก้ typo, ปรับ wording |

เมื่อ AI model ใหม่ออกมา ให้สร้าง experiment branch แล้วทดสอบ prompt เดิมกับ model ใหม่ หาก output ดีขึ้น ให้ update `ai-model` ใน Meta แล้ว PR กลับ main

---

*AI Work Library System v1.0 — Prompt-driven development workflow*
